<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.purple.demo.mapper.UserMapper">
	 <!-- 회원가입 -->
	<insert id="joinUser">
		INSERT INTO user
		( user_id, user_pw, user_name, user_email, user_birth)
		VALUES 
		( #{user_id}, #{user_pw},
		  #{user_name}, #{user_email}, #{user_birth} )
	</insert>
			
	<!-- 로그인 -->
	<select id="loginUser" resultType="com.purple.demo.model.UserEntity">
		SELECT *
		FROM user
		<where>
			<if test="user_id != null and user_id !=''">
				and user_id = #{user_id}
			</if>
			<if test="user_auth > 0">
				user_auth = #{user_auth}
			</if>
		</where>
	</select> 
	
	<!-- 아이디 중복체크 -->
	<select id="overlap_Confirm" resultType="Integer">
		SELECT COUNT(*)
		FROM user
		WHERE user_id = #{user_id}
	</select>
</mapper>